// Initialization for ES Users

import {
  Collapse,
  Dropdown,
  Sidenav,
  Ripple,
  Input,
  initTE,
  Datatable,
  Select,
} from "tw-elements";

initTE({ Sidenav, Ripple,Collapse, Dropdown, Datatable,Input,Select });

